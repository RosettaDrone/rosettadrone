/* AUTO-GENERATED FILE.  DO NOT MODIFY.
 *
 * This class was automatically generated by the
 * java mavlink generator tool. It should not be modified by hand.
 */

// MESSAGE OPEN_DRONE_ID_ARM_STATUS PACKING
package com.MAVLink.common;
import com.MAVLink.MAVLinkPacket;
import com.MAVLink.Messages.MAVLinkMessage;
import com.MAVLink.Messages.MAVLinkPayload;
import com.MAVLink.Messages.Units;
import com.MAVLink.Messages.Description;

/**
 * Transmitter (remote ID system) is enabled and ready to start sending location and other required information. This is streamed by transmitter. A flight controller uses it as a condition to arm.
 */
public class msg_open_drone_id_arm_status extends MAVLinkMessage {

    public static final int MAVLINK_MSG_ID_OPEN_DRONE_ID_ARM_STATUS = 12918;
    public static final int MAVLINK_MSG_LENGTH = 51;
    private static final long serialVersionUID = MAVLINK_MSG_ID_OPEN_DRONE_ID_ARM_STATUS;

    
    /**
     * Status level indicating if arming is allowed.
     */
    @Description("Status level indicating if arming is allowed.")
    @Units("")
    public short status;
    
    /**
     * Text error message, should be empty if status is good to arm. Fill with nulls in unused portion.
     */
    @Description("Text error message, should be empty if status is good to arm. Fill with nulls in unused portion.")
    @Units("")
    public byte error[] = new byte[50];
    

    /**
     * Generates the payload for a mavlink message for a message of this type
     * @return
     */
    @Override
    public MAVLinkPacket pack() {
        MAVLinkPacket packet = new MAVLinkPacket(MAVLINK_MSG_LENGTH,isMavlink2);
        packet.sysid = sysid;
        packet.compid = compid;
        packet.msgid = MAVLINK_MSG_ID_OPEN_DRONE_ID_ARM_STATUS;

        packet.payload.putUnsignedByte(status);
        
        for (int i = 0; i < error.length; i++) {
            packet.payload.putByte(error[i]);
        }
                    
        
        if (isMavlink2) {
            
        }
        return packet;
    }

    /**
     * Decode a open_drone_id_arm_status message into this class fields
     *
     * @param payload The message to decode
     */
    @Override
    public void unpack(MAVLinkPayload payload) {
        payload.resetIndex();

        this.status = payload.getUnsignedByte();
        
        for (int i = 0; i < this.error.length; i++) {
            this.error[i] = payload.getByte();
        }
                
        
        if (isMavlink2) {
            
        }
    }

    /**
     * Constructor for a new message, just initializes the msgid
     */
    public msg_open_drone_id_arm_status() {
        this.msgid = MAVLINK_MSG_ID_OPEN_DRONE_ID_ARM_STATUS;
    }

    /**
     * Constructor for a new message, initializes msgid and all payload variables
     */
    public msg_open_drone_id_arm_status( short status, byte[] error) {
        this.msgid = MAVLINK_MSG_ID_OPEN_DRONE_ID_ARM_STATUS;

        this.status = status;
        this.error = error;
        
    }

    /**
     * Constructor for a new message, initializes everything
     */
    public msg_open_drone_id_arm_status( short status, byte[] error, int sysid, int compid, boolean isMavlink2) {
        this.msgid = MAVLINK_MSG_ID_OPEN_DRONE_ID_ARM_STATUS;
        this.sysid = sysid;
        this.compid = compid;
        this.isMavlink2 = isMavlink2;

        this.status = status;
        this.error = error;
        
    }

    /**
     * Constructor for a new message, initializes the message with the payload
     * from a mavlink packet
     *
     */
    public msg_open_drone_id_arm_status(MAVLinkPacket mavLinkPacket) {
        this.msgid = MAVLINK_MSG_ID_OPEN_DRONE_ID_ARM_STATUS;

        this.sysid = mavLinkPacket.sysid;
        this.compid = mavLinkPacket.compid;
        this.isMavlink2 = mavLinkPacket.isMavlink2;
        unpack(mavLinkPacket.payload);
    }

       
    /**
    * Sets the buffer of this message with a string, adds the necessary padding
    */
    public void setError(String str) {
        int len = Math.min(str.length(), 50);
        for (int i=0; i<len; i++) {
            error[i] = (byte) str.charAt(i);
        }

        for (int i=len; i<50; i++) {            // padding for the rest of the buffer
            error[i] = 0;
        }
    }

    /**
    * Gets the message, formatted as a string
    */
    public String getError() {
        StringBuffer buf = new StringBuffer();
        for (int i = 0; i < 50; i++) {
            if (error[i] != 0)
                buf.append((char) error[i]);
            else
                break;
        }
        return buf.toString();

    }
                         
    /**
     * Returns a string with the MSG name and data
     */
    @Override
    public String toString() {
        return "MAVLINK_MSG_ID_OPEN_DRONE_ID_ARM_STATUS - sysid:"+sysid+" compid:"+compid+" status:"+status+" error:"+error+"";
    }

    /**
     * Returns a human-readable string of the name of the message
     */
    @Override
    public String name() {
        return "MAVLINK_MSG_ID_OPEN_DRONE_ID_ARM_STATUS";
    }
}
        